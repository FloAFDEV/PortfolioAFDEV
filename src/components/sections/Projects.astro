---
import ProjectCard from '../projects/ProjectCard.astro';
import { projects } from '../../data/projects';

const featured = projects.slice(0, 4);
---

<section id="projects" class="w-full glass-card px-6 py-8">
  <div class="flex items-center justify-between gap-4 mb-8 flex-wrap">
    <h2 class="section-title">Projets</h2>
    <a
      href="/projects"
      class="inline-flex items-center gap-1.5 text-sm font-semibold no-underline transition-all duration-200 cursor-pointer"
      style="color: #22C55E;"
    >
      Tous les projets
      <svg xmlns="http://www.w3.org/2000/svg" width="14" height="14" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true"><polyline points="9 18 15 12 9 6"/></svg>
    </a>
  </div>

  <ul class="list-none m-0 p-0 grid grid-cols-1 sm:grid-cols-2 gap-5 w-full">
    {featured.map((project) => (
      <li class="min-w-0 flex flex-col">
        <ProjectCard
          slug={project.slug}
          title={project.title}
          shortDescription={project.shortDescription}
          thumbnail={project.thumbnail}
          technologies={project.technologies}
          status={project.status}
        />
      </li>
    ))}
  </ul>
</section>
